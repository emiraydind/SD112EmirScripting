using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MyFirstScript_01 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        float floatVar = 5.5f;
        Debug.Log(floatVar);
       

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
